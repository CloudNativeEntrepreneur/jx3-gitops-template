gcactivities:
  podAnnotations:
    sidecar.istio.io/inject: "false"

gcpods:
  podAnnotations:
    sidecar.istio.io/inject: "false"

istio:
  enabled: true
  gateway:
    additionalHosts:
    - dashboard-jx.example.com
